<div class="container">
    <div class="row">
    <!--
    <script src = "/socket.io/socket.io.js"></script>
    <script>
      var socket = io();
      socket.on('login', function(data){document.write(data.description)});
      socket.emit('login', 'Sent an event from the client!');
      /ocket.on('broadcast',function(data) {
         document.body.innerHTML = '';
         document.write(data.description);
      });
   </script>
   -->
        <a href="/logout" class="btn btn-default">Çıkış Yap</a>
    </div>
</div>